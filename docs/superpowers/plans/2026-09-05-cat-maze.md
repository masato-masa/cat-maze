# 猫迷路（Cat Maze） 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 穴スライド式の盤面変形と猫の歩行を組み合わせた手数最適化パズルゲームを、ルールエンジン・ソルバー・UI まで含めて完成させる。

**Architecture:** 純粋関数のルールエンジン `src/core/` を中心に据え、ソルバー `src/solver/` と UI `src/ui/` がともにそれを利用する。ルール実装は1箇所にしか存在しない。UI は DOM + CSS transform で、2048（MIT）の実装パターンを流用する。

**Tech Stack:** TypeScript 5 / Vite / Vitest / 素の DOM + CSS（フレームワークなし）/ Node 24

**Spec:** `docs/superpowers/specs/2026-09-05-cat-maze-design.md`

## Global Constraints

- Node 24 / npm 11。パッケージマネージャは npm。
- `src/core/` は純粋関数のみ。`Math.random`・`Date`・`window`・`document` を参照しない。
- `src/solver/` は `src/core/` のルール関数をそのまま呼ぶ。ルールを再実装してはならない。
- `src/ui/` は `src/core/` の状態オブジェクトを直接書き換えない。必ず `core` の関数を通す。
- 状態はイミュータブル。すべての状態遷移関数は新しいオブジェクトを返す。
- 方角ビット: `N=1, E=2, S=4, W=8`（時計回り順。回転がビットローテートになる — Simon Tatham `net.c` の慣習）。
- ステージ定義の `optimalMoves` はソルバーが検証する。検証を通らないステージはコミットしない。
- 流用元の著作権表示を `THIRD_PARTY_NOTICES.md` に記載する。流用したファイルの先頭にも由来をコメントで書く。
- UI テキストは日本語。ひらがな中心（子どもも読める）。

## 流用元一覧

| 流用元 | ライセンス | 流用する内容 | 流用先 |
|---|---|---|---|
| [gabrielecirulli/2048](https://github.com/gabrielecirulli/2048) | MIT (c) 2014 Gabriele Cirulli | `keyboard_input_manager.js` のイベントエミッタ＋キーマップ＋スワイプ判定 | `src/ui/input.ts` |
| 同上 | MIT | `local_storage_manager.js` の fakeStorage フォールバックと利用可否プローブ | `src/ui/storage.ts` |
| 同上 | MIT | `html_actuator.js` の rAF 二段構え描画（前の位置で描いてから次フレームで位置更新しトランジションを起こす） | `src/ui/board-view.ts` |
| 同上 | MIT | `.game-container` / `.grid-container`（背景セル z-index 1）/ `.tile-container`（絶対配置タイル z-index 2）/ `.game-message`（オーバーレイ）の DOM・CSS 構造 | `src/ui/screens/game.ts`, `src/styles/board.css` |
| [Simon Tatham's Puzzles `net.c`](https://github.com/ghewgill/puzzles) | MIT | 方角ビットを時計回りに並べ回転をビットローテートで表現する慣習。中心から開いた辺へ線を引くパイプ描画 | `src/core/conn.ts`, `src/ui/tile-svg.ts` |

**流用しないもの**: BdR76/phaserlevelselect はライセンス表記が無く、同梱素材が Cut the Rope 由来のため、コードも素材も使用しない。ステージ選択画面は「未解放と星数を1つの進捗レコードで持つ」という一般的な考え方のみ参考にし、実装は自前で書く。

---

### Task 1: プロジェクト基盤と方角ビット演算

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `.gitignore`, `THIRD_PARTY_NOTICES.md`
- Create: `src/core/conn.ts`
- Test: `tests/core/conn.test.ts`

**Interfaces:**
- Consumes: なし
- Produces: `npm test` / `npm run dev` / `npm run build` が動く。`src/core/conn.ts` から
  `N, E, S, W: number`, `type Dir = 0|1|2|3`, `type Conn = number`, `ALL_DIRS: Dir[]`,
  `bit(d: Dir): number`, `opposite(d: Dir): Dir`, `delta(d: Dir): {dr:number, dc:number}`,
  `isOpen(c: Conn, d: Dir): boolean`, `rotateCW(c: Conn): Conn`,
  `parseConn(s: string): Conn`, `connToString(c: Conn): string`

- [ ] **Step 1: npm プロジェクトを作る**

```bash
npm init -y
npm i -D typescript vite vitest jsdom @types/node
```

`package.json` に `"type": "module"` を追加し、`scripts` を以下にする:

```json
{
  "dev": "vite",
  "build": "tsc --noEmit && vite build",
  "preview": "vite preview",
  "test": "vitest run",
  "test:watch": "vitest",
  "play": "node --experimental-strip-types src/cli/play.ts",
  "verify": "node --experimental-strip-types src/solver/cli.ts verify"
}
```

- [ ] **Step 2: `tsconfig.json` を書く**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "skipLibCheck": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vitest/globals"]
  },
  "include": ["src", "tests"]
}
```

`noUncheckedIndexedAccess` を有効にするのは、盤面配列の範囲外アクセスを型で潰すため。

- [ ] **Step 3: `vite.config.ts` と `.gitignore` を書く**

```ts
import { defineConfig } from 'vite';

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['tests/**/*.test.ts'],
  },
});
```

`.gitignore`:

```
node_modules/
dist/
.DS_Store
```

- [ ] **Step 4: `THIRD_PARTY_NOTICES.md` を書く**

2048 の MIT ライセンス全文（`Copyright (c) 2014 Gabriele Cirulli`）と、Simon Tatham's Portable Puzzle Collection の MIT ライセンス表記を記載する。それぞれ「どのファイルで何を流用したか」を1行添える。

- [ ] **Step 5: 失敗するテストを書く**

`tests/core/conn.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { N, E, S, W, opposite, delta, rotateCW, parseConn, connToString } from '../../src/core/conn.ts';

describe('conn', () => {
  it('方角ビットは時計回りに並ぶ', () => {
    expect([N, E, S, W]).toEqual([1, 2, 4, 8]);
  });

  it('opposite は反対の方角を返す', () => {
    expect(opposite(0)).toBe(2);
    expect(opposite(1)).toBe(3);
    expect(opposite(2)).toBe(0);
    expect(opposite(3)).toBe(1);
  });

  it('delta は行と列の増分を返す', () => {
    expect(delta(0)).toEqual({ dr: -1, dc: 0 });
    expect(delta(1)).toEqual({ dr: 0, dc: 1 });
    expect(delta(2)).toEqual({ dr: 1, dc: 0 });
    expect(delta(3)).toEqual({ dr: 0, dc: -1 });
  });

  it('rotateCW はビットローテートになる', () => {
    expect(rotateCW(N)).toBe(E);
    expect(rotateCW(W)).toBe(N);
    expect(rotateCW(N | S)).toBe(E | W);
    expect(rotateCW(0b1111)).toBe(0b1111);
  });

  it('parseConn は方角文字列を読む', () => {
    expect(parseConn('NS')).toBe(N | S);
    expect(parseConn('X')).toBe(0);
    expect(parseConn('NESW')).toBe(15);
  });

  it('connToString は parseConn の逆になる', () => {
    for (let c = 0; c < 16; c++) expect(parseConn(connToString(c))).toBe(c);
  });
});
```

- [ ] **Step 6: テストが落ちることを確認**

Run: `npm test`
Expected: FAIL（`src/core/conn.ts` が存在しない）

- [ ] **Step 7: `src/core/conn.ts` を実装**

```ts
// 方角ビットの並びと、回転をビットローテートで表す方式は
// Simon Tatham's Portable Puzzle Collection の net.c (MIT) の慣習に倣う。
// https://github.com/ghewgill/puzzles

export const N = 1;
export const E = 2;
export const S = 4;
export const W = 8;

/** 0=N, 1=E, 2=S, 3=W（時計回り） */
export type Dir = 0 | 1 | 2 | 3;
/** 4bit のビットマスク（0..15） */
export type Conn = number;

export const ALL_DIRS: Dir[] = [0, 1, 2, 3];

const BITS = [N, E, S, W] as const;
const LETTERS = ['N', 'E', 'S', 'W'] as const;
const DELTAS = [
  { dr: -1, dc: 0 },
  { dr: 0, dc: 1 },
  { dr: 1, dc: 0 },
  { dr: 0, dc: -1 },
] as const;

export function bit(d: Dir): number { return BITS[d]; }
export function opposite(d: Dir): Dir { return ((d + 2) % 4) as Dir; }
export function delta(d: Dir): { dr: number; dc: number } { return DELTAS[d]; }
export function isOpen(c: Conn, d: Dir): boolean { return (c & BITS[d]) !== 0; }

/** 時計回りに 90 度回転。ビットを1つ左へローテートするだけ。 */
export function rotateCW(c: Conn): Conn { return ((c << 1) | (c >> 3)) & 0xf; }

export function parseConn(s: string): Conn {
  if (s === 'X' || s === '') return 0;
  let c = 0;
  for (const ch of s) {
    const i = LETTERS.indexOf(ch as (typeof LETTERS)[number]);
    if (i < 0) throw new Error(`不正な方角文字: ${ch} (in "${s}")`);
    c |= BITS[i];
  }
  return c;
}

export function connToString(c: Conn): string {
  const s = LETTERS.filter((_, i) => (c & BITS[i]) !== 0).join('');
  return s === '' ? 'X' : s;
}
```

- [ ] **Step 8: テストが通ることを確認**

Run: `npm test`
Expected: PASS（6 tests）

- [ ] **Step 9: コミット**

```bash
git add -A
git commit -m "feat: プロジェクト基盤と方角ビット演算を追加"
```

---

### Task 2: 盤面と通行判定

**Files:**
- Create: `src/core/types.ts`, `src/core/board.ts`
- Test: `tests/core/board.test.ts`

**Interfaces:**
- Consumes: Task 1 の `conn.ts`
- Produces:
  - `types.ts`: `Pos`, `TileKind`, `Tile`, `Board`, `LevelDef`, `GameState`
  - `board.ts`: `createBoard(def: LevelDef): Board`, `idx(b, r, c): number`,
    `inBounds(b, r, c): boolean`, `tileAt(b, r, c): Tile | null`,
    `isHole(b, r, c): boolean`, `holes(b): Pos[]`,
    `passable(b, from: Pos, d: Dir): boolean`, `reachable(b, from: Pos): Set<number>`,
    `posEq(a: Pos, p: Pos): boolean`

- [ ] **Step 1: 失敗するテストを書く**

`tests/core/board.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { createBoard, passable, reachable, isHole, holes, idx } from '../../src/core/board.ts';
import type { LevelDef } from '../../src/core/types.ts';

/** 3x3。中央が穴。上段は横に繋がる。 */
const def: LevelDef = {
  id: 'T1', name: 'test', width: 3, height: 3,
  layout: [
    ['E', 'EW', 'W'],
    ['NS', 'HOLE', 'X'],
    ['FIXED:N', 'X', 'X'],
  ],
  catStart: [0, 0], optimalMoves: 0, parMoves: 0,
};

describe('board', () => {
  it('layout から盤面を作る', () => {
    const b = createBoard(def);
    expect(b.width).toBe(3);
    expect(b.height).toBe(3);
    expect(isHole(b, 1, 1)).toBe(true);
    expect(holes(b)).toEqual([{ r: 1, c: 1 }]);
    expect(b.cells[idx(b, 2, 0)]!.fixed).toBe(true);
  });

  it('両側が開いていれば通行できる', () => {
    const b = createBoard(def);
    expect(passable(b, { r: 0, c: 0 }, 1)).toBe(true);
    expect(passable(b, { r: 0, c: 1 }, 1)).toBe(true);
  });

  it('片側しか開いていなければ通行できない', () => {
    const b = createBoard(def);
    // (1,0) は NS で北へ開くが、(0,0) は E のみで南へ開いていない
    expect(passable(b, { r: 1, c: 0 }, 0)).toBe(false);
  });

  it('穴へは進入できない', () => {
    const b = createBoard(def);
    expect(passable(b, { r: 0, c: 1 }, 2)).toBe(false);
  });

  it('盤外へは進入できない', () => {
    const b = createBoard(def);
    expect(passable(b, { r: 0, c: 0 }, 0)).toBe(false);
  });

  it('到達領域は連結成分を返す', () => {
    const b = createBoard(def);
    const set = reachable(b, { r: 0, c: 0 });
    expect(set.has(idx(b, 0, 0))).toBe(true);
    expect(set.has(idx(b, 0, 1))).toBe(true);
    expect(set.has(idx(b, 0, 2))).toBe(true);
    expect(set.has(idx(b, 1, 0))).toBe(false);
    expect(set.size).toBe(3);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- board`
Expected: FAIL（モジュールが存在しない）

- [ ] **Step 3: `src/core/types.ts` を実装**

```ts
import type { Conn } from './conn.ts';

export type Pos = { r: number; c: number };
export type TileKind = 'road' | 'goal';

export type Tile = {
  /** 不変の識別子。UI がアニメーションでタイルを追跡するために使う。 */
  id: number;
  conn: Conn;
  kind: TileKind;
  /** true ならスライドできない。ゴールタイルを固定することもできる（仕様書 §3.3）。 */
  fixed: boolean;
  fish: boolean;
};

export type Board = {
  width: number;
  height: number;
  /** 行優先の一次元配列。null は穴。穴の位置はここからのみ導出する。 */
  cells: (Tile | null)[];
};

export type LevelDef = {
  id: string;
  name: string;
  width: number;
  height: number;
  /** 各要素は "NS" 形式の方角文字列。"X"=空き地、"HOLE"=穴。
   *  "FIXED:" / "GOAL:" / "FISH:" は前置修飾子（重ねてよい）。 */
  layout: string[][];
  catStart: [number, number];
  optimalMoves: number;
  parMoves: number;
  hint?: string;
};

export type GameState = {
  board: Board;
  cat: Pos;
  moves: number;
  fishTaken: number;
  fishTotal: number;
  cleared: boolean;
};
```

- [ ] **Step 4: `src/core/board.ts` を実装**

```ts
import { parseConn, isOpen, opposite, delta, ALL_DIRS } from './conn.ts';
import type { Dir } from './conn.ts';
import type { Board, LevelDef, Pos, Tile, TileKind } from './types.ts';

export function idx(b: Board, r: number, c: number): number { return r * b.width + c; }
export function posEq(a: Pos, p: Pos): boolean { return a.r === p.r && a.c === p.c; }

export function inBounds(b: Board, r: number, c: number): boolean {
  return r >= 0 && r < b.height && c >= 0 && c < b.width;
}

export function tileAt(b: Board, r: number, c: number): Tile | null {
  if (!inBounds(b, r, c)) return null;
  return b.cells[idx(b, r, c)] ?? null;
}

export function isHole(b: Board, r: number, c: number): boolean {
  return inBounds(b, r, c) && b.cells[idx(b, r, c)] == null;
}

export function holes(b: Board): Pos[] {
  const out: Pos[] = [];
  for (let r = 0; r < b.height; r++)
    for (let c = 0; c < b.width; c++)
      if (b.cells[idx(b, r, c)] == null) out.push({ r, c });
  return out;
}

/**
 * "FIXED:NS" のような1セル分の記述をタイルに変換する。"HOLE" なら null。
 * 修飾子は重ねられる: "FIXED:GOAL:NW" は動かないゴール。
 */
function parseCell(spec: string, id: number): Tile | null {
  if (spec === 'HOLE') return null;
  let kind: TileKind = 'road';
  let fixed = false;
  let fish = false;
  let rest = spec;
  for (;;) {
    const m = /^(FIXED|GOAL|FISH):(.*)$/.exec(rest);
    if (!m) break;
    if (m[1] === 'FIXED') fixed = true;
    else if (m[1] === 'GOAL') kind = 'goal';
    else fish = true;
    rest = m[2]!;
  }
  return { id, conn: parseConn(rest), kind, fixed, fish };
}

export function createBoard(def: LevelDef): Board {
  const cells: (Tile | null)[] = [];
  let id = 0;
  for (let r = 0; r < def.height; r++) {
    const row = def.layout[r];
    if (!row || row.length !== def.width)
      throw new Error(`layout の ${r} 行目の長さが width と一致しない`);
    for (let c = 0; c < def.width; c++) cells.push(parseCell(row[c]!, id++));
  }
  return { width: def.width, height: def.height, cells };
}

export function passable(b: Board, from: Pos, d: Dir): boolean {
  const { dr, dc } = delta(d);
  const a = tileAt(b, from.r, from.c);
  const t = tileAt(b, from.r + dr, from.c + dc);
  if (!a || !t) return false; // 盤外または穴
  return isOpen(a.conn, d) && isOpen(t.conn, opposite(d));
}

/** 猫が歩行のみで到達できるセルの集合（idx の集合）。 */
export function reachable(b: Board, from: Pos): Set<number> {
  const seen = new Set<number>();
  if (!tileAt(b, from.r, from.c)) return seen;
  const stack: Pos[] = [from];
  seen.add(idx(b, from.r, from.c));
  while (stack.length) {
    const p = stack.pop()!;
    for (const d of ALL_DIRS) {
      if (!passable(b, p, d)) continue;
      const { dr, dc } = delta(d);
      const q = { r: p.r + dr, c: p.c + dc };
      const k = idx(b, q.r, q.c);
      if (seen.has(k)) continue;
      seen.add(k);
      stack.push(q);
    }
  }
  return seen;
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add -A
git commit -m "feat: 盤面の生成と通行判定・到達領域を追加"
```

---

### Task 3: 穴スライドと猫の運搬

**Files:**
- Create: `src/core/slide.ts`
- Test: `tests/core/slide.test.ts`

**Interfaces:**
- Consumes: `board.ts`, `types.ts`
- Produces:
  - `holeFor(b: Board, target: Pos): Pos | null` — 対象タイルが押し込まれる穴
  - `canSlide(b: Board, target: Pos): boolean`
  - `slideTargets(b: Board): Pos[]` — 押せるタイル一覧（ソルバーの手生成に使う）
  - `applySlide(b: Board, cat: Pos, target: Pos): { board: Board; cat: Pos }`

- [ ] **Step 1: 失敗するテストを書く**

`tests/core/slide.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { createBoard, isHole, tileAt } from '../../src/core/board.ts';
import { canSlide, applySlide, slideTargets } from '../../src/core/slide.ts';
import type { LevelDef } from '../../src/core/types.ts';

/** 3x3。穴は (1,1)。(1,2) は fixed。 */
const def: LevelDef = {
  id: 'T2', name: 'slide', width: 3, height: 3,
  layout: [
    ['N', 'E', 'S'],
    ['W', 'HOLE', 'FIXED:N'],
    ['NE', 'ES', 'SW'],
  ],
  catStart: [1, 0], optimalMoves: 0, parMoves: 0,
};

const line = (cells: string[], w: number): LevelDef => ({
  id: 'L', name: 'line', width: w, height: 1,
  layout: [cells], catStart: [0, 0], optimalMoves: 0, parMoves: 0,
});

describe('slide', () => {
  it('穴と同じ行・列のタイルは押せる', () => {
    const b = createBoard(def);
    expect(canSlide(b, { r: 1, c: 0 })).toBe(true);
    expect(canSlide(b, { r: 0, c: 1 })).toBe(true);
  });

  it('穴と同じ行・列でないタイルは押せない', () => {
    const b = createBoard(def);
    expect(canSlide(b, { r: 0, c: 0 })).toBe(false);
    expect(canSlide(b, { r: 2, c: 2 })).toBe(false);
  });

  it('固定タイル自身は押せない', () => {
    expect(canSlide(createBoard(def), { r: 1, c: 2 })).toBe(false);
  });

  it('穴自身は押せない', () => {
    expect(canSlide(createBoard(def), { r: 1, c: 1 })).toBe(false);
  });

  it('押すと穴が対象タイルの位置へ移る', () => {
    const b = createBoard(def);
    const r = applySlide(b, { r: 2, c: 2 }, { r: 1, c: 0 });
    expect(isHole(r.board, 1, 0)).toBe(true);
    expect(isHole(r.board, 1, 1)).toBe(false);
    expect(tileAt(r.board, 1, 1)!.id).toBe(tileAt(b, 1, 0)!.id);
  });

  it('猫が乗ったタイルを押すと猫も一緒に運ばれる', () => {
    const b = createBoard(def);
    const r = applySlide(b, { r: 1, c: 0 }, { r: 1, c: 0 });
    expect(r.cat).toEqual({ r: 1, c: 1 });
  });

  it('対象区間の外にいる猫は動かない', () => {
    const b = createBoard(def);
    const r = applySlide(b, { r: 2, c: 0 }, { r: 1, c: 0 });
    expect(r.cat).toEqual({ r: 2, c: 0 });
  });

  it('離れたタイルを押すと間のタイルもまとめて動く', () => {
    const b = createBoard(line(['HOLE', 'N', 'E', 'S'], 4));
    const r = applySlide(b, { r: 0, c: 3 }, { r: 0, c: 3 });
    expect(isHole(r.board, 0, 3)).toBe(true);
    expect(tileAt(r.board, 0, 0)!.id).toBe(tileAt(b, 0, 1)!.id);
    expect(tileAt(r.board, 0, 1)!.id).toBe(tileAt(b, 0, 2)!.id);
    expect(tileAt(r.board, 0, 2)!.id).toBe(tileAt(b, 0, 3)!.id);
    expect(r.cat).toEqual({ r: 0, c: 2 }); // 猫も1マス運ばれる
  });

  it('固定タイルを越えて押すことはできない', () => {
    const b = createBoard(line(['HOLE', 'N', 'FIXED:E', 'S'], 4));
    expect(canSlide(b, { r: 0, c: 1 })).toBe(true);
    expect(canSlide(b, { r: 0, c: 3 })).toBe(false);
  });

  it('両側に同じ距離の穴があるタイルは曖昧なので押せない', () => {
    const b = createBoard(line(['HOLE', 'N', 'HOLE'], 3));
    expect(canSlide(b, { r: 0, c: 1 })).toBe(false);
  });

  it('スライドは可逆', () => {
    const b = createBoard(def);
    const a = applySlide(b, { r: 1, c: 0 }, { r: 1, c: 0 });
    const back = applySlide(a.board, a.cat, { r: 1, c: 1 });
    expect(back.board.cells.map((t) => t?.id ?? -1)).toEqual(b.cells.map((t) => t?.id ?? -1));
    expect(back.cat).toEqual({ r: 1, c: 0 });
  });

  it('slideTargets は押せるタイルをすべて返す', () => {
    // 行: (1,0) のみ（(1,2) は fixed）。列: (0,1), (2,1)。
    expect(slideTargets(createBoard(def))).toHaveLength(3);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- slide`
Expected: FAIL

- [ ] **Step 3: `src/core/slide.ts` を実装**

```ts
import { holes, idx, tileAt } from './board.ts';
import type { Board, Pos } from './types.ts';

function sign(n: number): number { return n > 0 ? 1 : n < 0 ? -1 : 0; }

/**
 * 対象タイルが押し込まれる穴を返す。
 * 同じ行/列にある穴のうち最も近いものを選ぶ。両側同距離なら曖昧なので null。
 * 対象タイルと穴の間に固定タイルがある穴は候補から外す。
 */
export function holeFor(b: Board, target: Pos): Pos | null {
  const t = tileAt(b, target.r, target.c);
  if (!t || t.fixed) return null;

  const cands: { h: Pos; dist: number }[] = [];
  for (const h of holes(b)) {
    if (h.r !== target.r && h.c !== target.c) continue;
    const dr = sign(h.r - target.r);
    const dc = sign(h.c - target.c);
    const dist = Math.abs(h.r - target.r) + Math.abs(h.c - target.c);
    let blocked = false;
    for (let r = target.r + dr, c = target.c + dc; !(r === h.r && c === h.c); r += dr, c += dc) {
      const mid = tileAt(b, r, c);
      if (!mid || mid.fixed) { blocked = true; break; }
    }
    if (!blocked) cands.push({ h, dist });
  }
  if (cands.length === 0) return null;
  cands.sort((a, z) => a.dist - z.dist);
  if (cands.length > 1 && cands[0]!.dist === cands[1]!.dist) return null; // 曖昧
  return cands[0]!.h;
}

export function canSlide(b: Board, target: Pos): boolean {
  return holeFor(b, target) !== null;
}

export function slideTargets(b: Board): Pos[] {
  const out: Pos[] = [];
  for (let r = 0; r < b.height; r++)
    for (let c = 0; c < b.width; c++)
      if (canSlide(b, { r, c })) out.push({ r, c });
  return out;
}

/**
 * 対象タイルから穴までの区間を穴側へ1マスずつ詰める。
 * 区間内に猫が乗っていれば、猫はタイルと一緒に運ばれる。
 */
export function applySlide(b: Board, cat: Pos, target: Pos): { board: Board; cat: Pos } {
  const hole = holeFor(b, target);
  if (!hole) throw new Error(`押せないタイル: (${target.r}, ${target.c})`);

  const cells = b.cells.slice();
  const board: Board = { width: b.width, height: b.height, cells };
  let newCat = cat;

  // タイルが動く向き（対象タイル -> 穴）
  const dr = sign(hole.r - target.r);
  const dc = sign(hole.c - target.c);

  // 穴の1つ手前から対象タイルへ向かって、1つずつ穴側へ詰める
  for (let r = hole.r - dr, c = hole.c - dc; ; r -= dr, c -= dc) {
    cells[idx(board, r + dr, c + dc)] = cells[idx(board, r, c)]!;
    if (cat.r === r && cat.c === c) newCat = { r: r + dr, c: c + dc };
    if (r === target.r && c === target.c) break;
  }
  cells[idx(board, target.r, target.c)] = null; // ここが新しい穴

  return { board, cat: newCat };
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add -A
git commit -m "feat: 穴スライドと猫の運搬を実装"
```

---

### Task 4: ゲーム状態・歩行・勝利判定・Undo

**Files:**
- Create: `src/core/rules.ts`, `src/core/game.ts`
- Test: `tests/core/game.test.ts`

**Interfaces:**
- Consumes: `board.ts`, `slide.ts`, `types.ts`
- Produces:
  - `rules.ts`: `countFish(b: Board): number`, `isCleared(s: GameState): boolean`,
    `starsFor(def: LevelDef, moves: number): 1 | 2 | 3`
  - `game.ts`: `newGame(def): GameState`, `canWalk(s, d): boolean`,
    `walk(s, d): GameState`（歩けなければ同一オブジェクト）, `reachableSet(s): Set<number>`,
    `shortestPath(s, to): Dir[] | null`, `walkTo(s, to): GameState`,
    `slide(s, target): GameState`, `class GameSession`

- [ ] **Step 1: 失敗するテストを書く**

`tests/core/game.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { newGame, walk, slide, GameSession, shortestPath } from '../../src/core/game.ts';
import { starsFor } from '../../src/core/rules.ts';
import type { LevelDef } from '../../src/core/types.ts';

/** 1x4。猫 -> 魚 -> ゴール が横一列。穴は右端。 */
const def: LevelDef = {
  id: 'T3', name: 'game', width: 4, height: 1,
  layout: [['E', 'FISH:EW', 'GOAL:W', 'HOLE']],
  catStart: [0, 0], optimalMoves: 0, parMoves: 2,
};

describe('game', () => {
  it('初期状態を作る', () => {
    const s = newGame(def);
    expect(s.cat).toEqual({ r: 0, c: 0 });
    expect(s.moves).toBe(0);
    expect(s.fishTotal).toBe(1);
    expect(s.fishTaken).toBe(0);
    expect(s.cleared).toBe(false);
  });

  it('歩行では手数が増えない', () => {
    const s = walk(newGame(def), 1);
    expect(s.cat).toEqual({ r: 0, c: 1 });
    expect(s.moves).toBe(0);
  });

  it('歩けない方向へは動かない', () => {
    const s = newGame(def);
    expect(walk(s, 3)).toBe(s);
  });

  it('魚のマスに入ると取得する', () => {
    expect(walk(newGame(def), 1).fishTaken).toBe(1);
  });

  it('魚を取らずにゴールに乗ってもクリアにならない', () => {
    const d: LevelDef = { ...def, layout: [['FISH:E', 'EW', 'GOAL:W', 'HOLE']], catStart: [0, 2] };
    const s = newGame(d);
    expect(s.cat).toEqual({ r: 0, c: 2 });
    expect(s.fishTotal).toBe(1);
    expect(s.cleared).toBe(false);
  });

  it('魚を全部取ってゴールに乗るとクリア', () => {
    let s = newGame(def);
    s = walk(s, 1);
    s = walk(s, 1);
    expect(s.fishTaken).toBe(1);
    expect(s.cleared).toBe(true);
  });

  it('スライドで手数が増える', () => {
    expect(slide(newGame(def), { r: 0, c: 2 }).moves).toBe(1);
  });

  it('shortestPath は到達可能なら経路を返す', () => {
    expect(shortestPath(newGame(def), { r: 0, c: 2 })).toEqual([1, 1]);
    expect(shortestPath(newGame(def), { r: 0, c: 3 })).toBeNull();
  });

  it('Undo でスライド前の状態に戻る', () => {
    const sess = new GameSession(def);
    sess.walk(1);
    const before = sess.current;
    sess.slide({ r: 0, c: 2 });
    expect(sess.current.moves).toBe(1);
    sess.undo();
    expect(sess.current.moves).toBe(0);
    expect(sess.current.cat).toEqual(before.cat);
  });

  it('歩行だけでは Undo できない', () => {
    const sess = new GameSession(def);
    sess.walk(1);
    expect(sess.canUndo).toBe(false);
  });

  it('reset で初期状態に戻る', () => {
    const sess = new GameSession(def);
    sess.slide({ r: 0, c: 2 });
    sess.reset();
    expect(sess.current.moves).toBe(0);
    expect(sess.current.cat).toEqual({ r: 0, c: 0 });
    expect(sess.canUndo).toBe(false);
  });

  it('星は手数で決まる', () => {
    const d = { ...def, optimalMoves: 3, parMoves: 5 };
    expect(starsFor(d, 3)).toBe(3);
    expect(starsFor(d, 4)).toBe(2);
    expect(starsFor(d, 5)).toBe(2);
    expect(starsFor(d, 6)).toBe(1);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- game`
Expected: FAIL

- [ ] **Step 3: `src/core/rules.ts` を実装**

```ts
import { tileAt } from './board.ts';
import type { Board, GameState, LevelDef } from './types.ts';

export function countFish(b: Board): number {
  return b.cells.reduce((n, t) => n + (t?.fish ? 1 : 0), 0);
}

export function isCleared(s: GameState): boolean {
  const t = tileAt(s.board, s.cat.r, s.cat.c);
  return !!t && t.kind === 'goal' && s.fishTaken >= s.fishTotal;
}

export function starsFor(def: LevelDef, moves: number): 1 | 2 | 3 {
  if (moves <= def.optimalMoves) return 3;
  if (moves <= def.parMoves) return 2;
  return 1;
}
```

- [ ] **Step 4: `src/core/game.ts` を実装**

```ts
import { createBoard, idx, passable, reachable, tileAt } from './board.ts';
import { ALL_DIRS, delta } from './conn.ts';
import type { Dir } from './conn.ts';
import { applySlide } from './slide.ts';
import { countFish, isCleared } from './rules.ts';
import type { Board, GameState, LevelDef, Pos } from './types.ts';

/** 猫が乗ったマスの魚を回収した新しい状態を返す。 */
function pickUp(s: GameState): GameState {
  const t = tileAt(s.board, s.cat.r, s.cat.c);
  if (!t || !t.fish) return s;
  const cells = s.board.cells.slice();
  cells[idx(s.board, s.cat.r, s.cat.c)] = { ...t, fish: false };
  const board: Board = { ...s.board, cells };
  return { ...s, board, fishTaken: s.fishTaken + 1 };
}

/** 魚の回収とクリア判定を反映させる。状態遷移の最後に必ず通す。 */
function settle(s: GameState): GameState {
  const picked = pickUp(s);
  const cleared = isCleared(picked);
  return cleared === picked.cleared ? picked : { ...picked, cleared };
}

export function newGame(def: LevelDef): GameState {
  const board = createBoard(def);
  return settle({
    board,
    cat: { r: def.catStart[0], c: def.catStart[1] },
    moves: 0,
    fishTaken: 0,
    fishTotal: countFish(board),
    cleared: false,
  });
}

export function canWalk(s: GameState, d: Dir): boolean {
  return passable(s.board, s.cat, d);
}

/** 歩行。手数は増えない。歩けない場合は同一オブジェクトを返す。 */
export function walk(s: GameState, d: Dir): GameState {
  if (!canWalk(s, d)) return s;
  const { dr, dc } = delta(d);
  return settle({ ...s, cat: { r: s.cat.r + dr, c: s.cat.c + dc } });
}

export function reachableSet(s: GameState): Set<number> {
  return reachable(s.board, s.cat);
}

/** 猫から to までの最短経路を方向の列で返す。到達不能なら null。 */
export function shortestPath(s: GameState, to: Pos): Dir[] | null {
  const b = s.board;
  const start = idx(b, s.cat.r, s.cat.c);
  const goal = idx(b, to.r, to.c);
  if (start === goal) return [];
  const prev = new Map<number, { from: number; d: Dir }>();
  const queue: Pos[] = [s.cat];
  const seen = new Set<number>([start]);
  while (queue.length) {
    const p = queue.shift()!;
    for (const d of ALL_DIRS) {
      if (!passable(b, p, d)) continue;
      const { dr, dc } = delta(d);
      const q = { r: p.r + dr, c: p.c + dc };
      const k = idx(b, q.r, q.c);
      if (seen.has(k)) continue;
      seen.add(k);
      prev.set(k, { from: idx(b, p.r, p.c), d });
      if (k === goal) {
        const out: Dir[] = [];
        for (let cur = k; cur !== start; ) {
          const e = prev.get(cur)!;
          out.push(e.d);
          cur = e.from;
        }
        return out.reverse();
      }
      queue.push(q);
    }
  }
  return null;
}

/** 到達領域内の目的地まで歩く。経路上の魚も回収する。 */
export function walkTo(s: GameState, to: Pos): GameState {
  const path = shortestPath(s, to);
  if (!path) return s;
  let cur = s;
  for (const d of path) cur = walk(cur, d);
  return cur;
}

/** スライド。1手を消費する。 */
export function slide(s: GameState, target: Pos): GameState {
  const r = applySlide(s.board, s.cat, target);
  return settle({ ...s, board: r.board, cat: r.cat, moves: s.moves + 1 });
}

/**
 * Undo スタックを持つセッション。
 * スナップショットは1手（1スライド）単位。歩行は手数を消費しないため
 * 単独では Undo 点を作らず、直前のスライド直前の状態まで巻き戻る。
 */
export class GameSession {
  readonly def: LevelDef;
  private state: GameState;
  private history: GameState[] = [];

  constructor(def: LevelDef) {
    this.def = def;
    this.state = newGame(def);
  }

  get current(): GameState { return this.state; }
  get canUndo(): boolean { return this.history.length > 0; }

  walk(d: Dir): GameState { this.state = walk(this.state, d); return this.state; }
  walkTo(to: Pos): GameState { this.state = walkTo(this.state, to); return this.state; }

  slide(target: Pos): GameState {
    this.history.push(this.state);
    this.state = slide(this.state, target);
    return this.state;
  }

  undo(): GameState {
    const prev = this.history.pop();
    if (prev) this.state = prev;
    return this.state;
  }

  reset(): GameState {
    this.history = [];
    this.state = newGame(this.def);
    return this.state;
  }
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add -A
git commit -m "feat: ゲーム状態・歩行・勝利判定・Undo を実装"
```

---

### Task 5: ステージ定義スキーマと W1

**Files:**
- Create: `src/levels/schema.ts`, `src/levels/w1.ts`, `src/levels/index.ts`
- Test: `tests/levels/levels.test.ts`

**Interfaces:**
- Consumes: `core/types.ts`, `core/board.ts`, `core/game.ts`
- Produces:
  - `schema.ts`: `validateLevel(def: LevelDef): string[]`（問題点の配列。空なら妥当）
  - `index.ts`: `type World = { id, name, levels }`, `WORLDS: World[]`,
    `ALL_LEVELS: LevelDef[]`, `getLevel(id): LevelDef | undefined`,
    `nextLevelId(id): string | null`

- [ ] **Step 1: 失敗するテストを書く**

`tests/levels/levels.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { ALL_LEVELS, WORLDS, getLevel, nextLevelId } from '../../src/levels/index.ts';
import { validateLevel } from '../../src/levels/schema.ts';
import { newGame } from '../../src/core/game.ts';

describe('levels', () => {
  it('W1 は 6 ステージある', () => {
    expect(WORLDS[0]!.levels).toHaveLength(6);
  });

  it('すべてのステージが妥当', () => {
    for (const def of ALL_LEVELS) {
      expect(validateLevel(def), `${def.id}: ${validateLevel(def).join(' / ')}`).toEqual([]);
    }
  });

  it('ステージ ID は重複しない', () => {
    const ids = ALL_LEVELS.map((l) => l.id);
    expect(new Set(ids).size).toBe(ids.length);
  });

  it('初期状態でクリア済みのステージは無い', () => {
    for (const def of ALL_LEVELS) {
      expect(newGame(def).cleared, `${def.id} は最初からクリア済み`).toBe(false);
    }
  });

  it('getLevel は ID で引ける', () => {
    expect(getLevel('W1-1')!.id).toBe('W1-1');
    expect(getLevel('nope')).toBeUndefined();
  });

  it('nextLevelId は次のステージを返し、最後は null', () => {
    expect(nextLevelId('W1-1')).toBe('W1-2');
    expect(nextLevelId(ALL_LEVELS[ALL_LEVELS.length - 1]!.id)).toBeNull();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- levels`
Expected: FAIL

- [ ] **Step 3: `src/levels/schema.ts` を実装**

```ts
import { createBoard, holes, inBounds, tileAt } from '../core/board.ts';
import type { LevelDef } from '../core/types.ts';

export function validateLevel(def: LevelDef): string[] {
  const errs: string[] = [];
  if (def.layout.length !== def.height) errs.push('layout の行数が height と違う');
  def.layout.forEach((row, r) => {
    if (row.length !== def.width) errs.push(`${r} 行目の長さが width と違う`);
  });
  if (errs.length) return errs;

  let board;
  try { board = createBoard(def); }
  catch (e) { return [`パースできない: ${(e as Error).message}`]; }

  if (holes(board).length < 1) errs.push('穴が1つも無い');
  const goals = board.cells.filter((t) => t?.kind === 'goal').length;
  if (goals !== 1) errs.push(`ゴールが ${goals} 個ある（1個であるべき）`);
  const [cr, cc] = def.catStart;
  if (!inBounds(board, cr, cc)) errs.push('catStart が盤外');
  else if (!tileAt(board, cr, cc)) errs.push('catStart が穴の上');
  if (def.parMoves < def.optimalMoves) errs.push('parMoves が optimalMoves より小さい');
  return errs;
}
```

- [ ] **Step 4: `src/levels/w1.ts` に W1 の 6 ステージを書く**

仕様書 §9.4 の設計意図に従う。`optimalMoves` はこの時点では見積もり値を置き、Task 8 の検証で確定させる。

- `W1-1 はじめの一歩` 4×4 / 押せるタイルが実質1枚。押すと道が繋がる / 見積 1手
- `W1-2 のりもの` 4×4 / 猫の周囲が閉じていて歩けない。猫ごと運ぶしかない / 見積 1手
- `W1-3 ふたつ うごかす` 4×4 / 運搬 + 接続の組み合わせ / 見積 2手
- `W1-4 たちいち` 4×4 / 押す前の立ち位置で結果が変わる / 見積 2手
- `W1-5 あなの むこう` 4×4 / 穴が壁になっている。穴を動かす / 見積 3手
- `W1-6 うらにわ` 4×4 / 総合 / 見積 4手

- [ ] **Step 5: `src/levels/index.ts` を実装**

```ts
import type { LevelDef } from '../core/types.ts';
import { W1 } from './w1.ts';

export type World = { id: string; name: string; levels: LevelDef[] };

export const WORLDS: World[] = [
  { id: 'W1', name: 'うらにわ', levels: W1 },
];

export const ALL_LEVELS: LevelDef[] = WORLDS.flatMap((w) => w.levels);

export function getLevel(id: string): LevelDef | undefined {
  return ALL_LEVELS.find((l) => l.id === id);
}

export function nextLevelId(id: string): string | null {
  const i = ALL_LEVELS.findIndex((l) => l.id === id);
  if (i < 0 || i + 1 >= ALL_LEVELS.length) return null;
  return ALL_LEVELS[i + 1]!.id;
}
```

- [ ] **Step 6: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 7: コミット**

```bash
git add -A
git commit -m "feat: ステージ定義スキーマと W1 の6ステージを追加"
```

---

### Task 6: CLI プレイヤー（M2 チェックポイント）

**Files:**
- Create: `src/cli/render.ts`, `src/cli/play.ts`
- Test: `tests/cli/render.test.ts`

**Interfaces:**
- Consumes: `core/game.ts`, `core/slide.ts`, `levels/index.ts`
- Produces: `renderBoard(s: GameState, opts?: { targets?: Pos[] }): string`。`npm run play` で対話プレイできる。

**このタスクの目的**: UI を書く前に「一石二鳥の手を見つけたときの快感」が実在するかを確かめる。手応えが無ければ先へ進まずルールを見直す。

- [ ] **Step 1: 失敗するテストを書く**

`tests/cli/render.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { renderBoard } from '../../src/cli/render.ts';
import { newGame } from '../../src/core/game.ts';
import type { LevelDef } from '../../src/core/types.ts';

const def: LevelDef = {
  id: 'R1', name: 'render', width: 3, height: 1,
  layout: [['E', 'GOAL:EW', 'HOLE']],
  catStart: [0, 0], optimalMoves: 0, parMoves: 0,
};

describe('renderBoard', () => {
  it('猫・ゴール・穴が文字として現れる', () => {
    const out = renderBoard(newGame(def));
    expect(out).toContain('C');
    expect(out).toContain('G');
    expect(out).toContain('.');
  });

  it('行数は盤面の高さと一致する', () => {
    expect(renderBoard(newGame(def)).trimEnd().split('\n')).toHaveLength(1);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- render`
Expected: FAIL

- [ ] **Step 3: `src/cli/render.ts` を実装**

各セルを1文字で表す。`C`=猫、`G`=ゴール、`F`=魚、`#`=固定、`.`=穴。道は接続に応じた罫線文字を 16 通りのテーブルで引く（`─ │ ┌ ┐ └ ┘ ├ ┤ ┬ ┴ ┼` と行き止まり `╴ ╵ ╶ ╷`、空き地は空白）。

- [ ] **Step 4: `src/cli/play.ts` を実装**

`node:readline` の raw モードで1キーずつ受け取る対話プレイヤー。
- 矢印キー / WASD: 歩行
- `1`..`9`: `slideTargets()` の n 番目を押す（盤面の下に番号付きで一覧表示）
- `u`: Undo、`r`: リセット、`n`: 次のステージ、`q`: 終了
- 毎入力後に再描画し、`てすう 3 / さいたん 2` と到達領域のセル数を表示する

- [ ] **Step 5: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 6: 実際に遊んで手応えを確認する（M2 ゲート）**

Run: `npm run play`

W1-1 から W1-6 まで実際に解き、以下を確認する:
- W1-2 で「猫ごと運ばれる」ことに驚きがあるか
- W1-4 で「立ち位置が結果を変える」ことが体感できるか
- 見積もりより短い解が見つかってしまわないか

- [ ] **Step 7: コミット**

```bash
git add -A
git commit -m "feat: CLI プレイヤーを追加（ルールの手触り確認用）"
```

---

### Task 7: ソルバー

**Files:**
- Create: `src/solver/search.ts`
- Test: `tests/solver/search.test.ts`

**Interfaces:**
- Consumes: `core/board.ts`, `core/slide.ts`, `core/game.ts`
- Produces:
  - `stateKey(s: GameState): string` — 到達領域を正規化した状態キー
  - `solve(def: LevelDef, maxDepth: number): Pos[] | null` — 最短手順（無ければ null）
  - `hasSolutionWithin(def: LevelDef, depth: number): boolean`

**状態圧縮**: 猫の正確な位置は状態に含めない。歩行が0手なので、同じ盤面・同じ到達領域なら猫がどこに立っていても等価。到達領域に含まれる最小 idx を代表点に使う。

**ゴール判定**: 猫がゴールへ「歩いて行ける」時点で解けている（歩行は0手だから）。魚も同様に、残りの魚がすべて到達領域内にあれば歩くだけで回収できる。

- [ ] **Step 1: 失敗するテストを書く**

`tests/solver/search.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { solve, hasSolutionWithin, stateKey } from '../../src/solver/search.ts';
import { newGame, walk, slide } from '../../src/core/game.ts';
import type { LevelDef } from '../../src/core/types.ts';

/** 猫とゴールの間が穴で分断されている。1回のスライドで繋がる。 */
const oneMove: LevelDef = {
  id: 'S1', name: 'one', width: 3, height: 1,
  layout: [['E', 'HOLE', 'GOAL:W']],
  catStart: [0, 0], optimalMoves: 1, parMoves: 2,
};

/** すでに繋がっているので 0 手。 */
const zeroMove: LevelDef = {
  id: 'S0', name: 'zero', width: 3, height: 1,
  layout: [['E', 'EW', 'GOAL:W']],
  catStart: [0, 0], optimalMoves: 0, parMoves: 0,
};

describe('solver', () => {
  it('0 手で解けるステージを検出する', () => {
    expect(solve(zeroMove, 5)).toEqual([]);
  });

  it('1 手で解けるステージの手順を返す', () => {
    const sol = solve(oneMove, 5);
    expect(sol).not.toBeNull();
    expect(sol!).toHaveLength(1);
  });

  it('深さが足りなければ null を返す', () => {
    expect(solve(oneMove, 0)).toBeNull();
  });

  it('hasSolutionWithin は下界の証明に使える', () => {
    expect(hasSolutionWithin(oneMove, 0)).toBe(false);
    expect(hasSolutionWithin(oneMove, 1)).toBe(true);
  });

  it('猫の立ち位置が違っても到達領域が同じなら同じ状態キー', () => {
    const a = newGame(zeroMove);
    expect(stateKey(a)).toBe(stateKey(walk(a, 1)));
  });

  it('盤面が違えば状態キーも違う', () => {
    const a = newGame(oneMove);
    expect(stateKey(a)).not.toBe(stateKey(slide(a, { r: 0, c: 0 })));
  });

  it('返された手順を実行するとクリアできる状態になる', () => {
    let s = newGame(oneMove);
    for (const t of solve(oneMove, 5)!) s = slide(s, t);
    // 手順の実行後は歩くだけでゴールへ行ける
    let walked = s;
    for (let i = 0; i < 5; i++) walked = walk(walked, 1);
    expect(walked.cleared).toBe(true);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- search`
Expected: FAIL

- [ ] **Step 3: `src/solver/search.ts` を実装**

```ts
import { idx, reachable, tileAt } from '../core/board.ts';
import { newGame, slide } from '../core/game.ts';
import { slideTargets } from '../core/slide.ts';
import type { GameState, LevelDef, Pos } from '../core/types.ts';

/**
 * 状態キー。タイル配置 ＋ 猫の到達領域の代表点 ＋ 取得済み魚数。
 * 猫の正確な位置は含めない（歩行が 0 手なので到達領域が同じなら等価）。
 */
export function stateKey(s: GameState): string {
  const cells = s.board.cells
    .map((t) => (t == null ? '.' : `${t.conn}${t.kind[0]}${t.fixed ? "x" : ""}${t.fish ? "f" : ""}`))
    .join(',');
  const reach = reachable(s.board, s.cat);
  let rep = Infinity;
  for (const k of reach) if (k < rep) rep = k;
  return `${cells}|${rep}|${s.fishTaken}`;
}

/** 歩くだけでクリアできる状態か。 */
function solvedHere(s: GameState): boolean {
  const reach = reachable(s.board, s.cat);
  let goalReachable = false;
  for (let r = 0; r < s.board.height; r++) {
    for (let c = 0; c < s.board.width; c++) {
      const t = tileAt(s.board, r, c);
      if (!t) continue;
      const k = idx(s.board, r, c);
      if (t.fish && !reach.has(k)) return false;        // 届かない魚がある
      if (t.kind === 'goal' && reach.has(k)) goalReachable = true;
    }
  }
  return goalReachable;
}

/** 深さ制限付き DFS。転置表には「その残余深さで未解決だった」ことを記録する。 */
function dfs(s: GameState, depth: number, seen: Map<string, number>, path: Pos[]): Pos[] | null {
  if (solvedHere(s)) return path.slice();
  if (depth === 0) return null;

  const key = stateKey(s);
  const best = seen.get(key);
  if (best !== undefined && best >= depth) return null;
  seen.set(key, depth);

  for (const t of slideTargets(s.board)) {
    path.push(t);
    const found = dfs(slide(s, t), depth - 1, seen, path);
    path.pop();
    if (found) return found;
  }
  return null;
}

/** 反復深化。maxDepth まで探して最短手順を返す。 */
export function solve(def: LevelDef, maxDepth: number): Pos[] | null {
  const start = newGame(def);
  for (let d = 0; d <= maxDepth; d++) {
    const found = dfs(start, d, new Map(), []);
    if (found) return found;
  }
  return null;
}

export function hasSolutionWithin(def: LevelDef, depth: number): boolean {
  return solve(def, depth) !== null;
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add -A
git commit -m "feat: 反復深化ソルバーを追加"
```

---

### Task 8: ステージ検証 CLI と W1 の確定

**Files:**
- Create: `src/solver/cli.ts`
- Modify: `src/levels/w1.ts`（ソルバーの結果に合わせて `optimalMoves` を修正）
- Test: `tests/levels/verify.test.ts`

**Interfaces:**
- Consumes: `solver/search.ts`, `levels/index.ts`, `levels/schema.ts`
- Produces: `npm run verify` が全ステージを検証し、不一致があれば終了コード 1 を返す

- [ ] **Step 1: 失敗するテストを書く**

`tests/levels/verify.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { ALL_LEVELS } from '../../src/levels/index.ts';
import { solve } from '../../src/solver/search.ts';

describe('ステージの最短手数', () => {
  for (const def of ALL_LEVELS) {
    it(`${def.id} は ${def.optimalMoves} 手で解ける`, () => {
      const sol = solve(def, def.optimalMoves);
      expect(sol, `${def.id} は ${def.optimalMoves} 手では解けない`).not.toBeNull();
      expect(sol!.length).toBe(def.optimalMoves);
    });

    it(`${def.id} は ${def.optimalMoves - 1} 手では解けない`, () => {
      if (def.optimalMoves === 0) return;
      expect(solve(def, def.optimalMoves - 1)).toBeNull();
    });
  }
});
```

- [ ] **Step 2: テストを走らせ、実際の最短手数を確認する**

Run: `npm test -- verify`
Expected: 見積もりが誤っているステージで FAIL する。`src/levels/w1.ts` の `optimalMoves` を実際の値に直す。直した結果、設計意図（例: W1-2 は運搬1手で解ける）が壊れているステージは盤面自体を作り直す。

- [ ] **Step 3: `src/solver/cli.ts` を実装**

`node --experimental-strip-types src/solver/cli.ts verify` で全ステージを検証し、
`W1-1  OK        optimal=1 par=3` の形式で1行ずつ出力する。
宣言値と実際が違えば `W1-3  MISMATCH  declared=3 actual=2` と出して終了コード 1。
`validateLevel` の問題点も同時に報告する。

サブコマンド `solve <levelId>` で1ステージの最短手順を出力する（ヒント機能の裏付けとデバッグ用）。

- [ ] **Step 4: 検証が通ることを確認**

Run: `npm run verify`
Expected: 全ステージ OK、終了コード 0

- [ ] **Step 5: コミット**

```bash
git add -A
git commit -m "feat: ステージ検証 CLI を追加し W1 の最短手数を確定"
```

---

### Task 9: W2〜W5 の作成

**Files:**
- Create: `src/levels/w2.ts`, `src/levels/w3.ts`, `src/levels/w4.ts`, `src/levels/w5.ts`
- Modify: `src/levels/index.ts`

**Interfaces:**
- Consumes: Task 5 のスキーマ、Task 8 の検証 CLI
- Produces: `WORLDS` が 5 ワールド・全 32 ステージになる

各ワールドは仕様書 §9.2 の「導入 → 練習 → 結合 → 反転 → 総括」に従う。

- [ ] **Step 1: W2「いしだたみ」6ステージを書く**

主題は固定タイル。固定タイルが押せる範囲を切ることを段階的に教える。4×4〜5×5、3〜7手。

- [ ] **Step 2: W2 を検証して確定させる**

Run: `npm run verify`
Expected: W2 が全て OK。想定と違えば盤面を調整する。

- [ ] **Step 3: コミット**

```bash
git add -A && git commit -m "feat: W2 いしだたみ 6ステージを追加"
```

- [ ] **Step 4: W3「さかなつり」6ステージを書いて検証・コミット**

主題は魚の収集。回収順で手数が変わる構成にする。5×5、5〜10手。

- [ ] **Step 5: W4「うごくおうち」6ステージを書いて検証・コミット**

主題は動くゴール。第1ステージは「猫がゴールへ歩いて行くことが原理的に不可能で、ゴールを猫の側へ運ぶしかない」構成にして発想の転換を強制する。5×5、6〜12手。

- [ ] **Step 6: W5「やねのうえ」8ステージを書いて検証・コミット**

総合。5×5〜6×6、8〜16手。ソルバーの探索時間が伸びるので固定タイルを多めに置いて分岐を抑える。1ステージの検証が 60 秒を超える場合は盤面を作り直す。

- [ ] **Step 7: 全ステージの最終検証**

Run: `npm run verify && npm test`
Expected: 32 ステージすべて OK

---

### Task 10: UI 基盤（進捗ストレージ・画面遷移）

**Files:**
- Create: `index.html`, `src/main.ts`, `src/ui/storage.ts`, `src/ui/router.ts`, `src/styles/base.css`
- Modify: `vite.config.ts`（`tests/ui/**` を jsdom 環境にする）
- Test: `tests/ui/storage.test.ts`

**Interfaces:**
- Consumes: `levels/index.ts`
- Produces:
  - `storage.ts`: `interface KV { get(k: string): string | undefined; set(k: string, v: string): void; delete(k: string): void }`,
    `browserKV(): KV`, `class ProgressStore` —
    `getStars(levelId): 0|1|2|3`, `bestMoves(levelId): number | null`,
    `isUnlocked(levelId): boolean`, `record(levelId, stars, moves): void`,
    `totalStars(): number`, `worldStars(worldId): number`,
    `firstUnclearedId(): string`, `clear(): void`
  - `router.ts`: `type Route = { screen: 'home' } | { screen: 'select' } | { screen: 'play'; levelId: string }`,
    `class Router` — `start()`, `go(route)`, `onChange(cb)`

**進捗モデル**: `{ [levelId]: { stars: 1|2|3, moves: number } }` をキー `catmaze_progress` で保存する。配列ではなくキー付きオブジェクトにするのは、ステージを後から増やしても壊れないようにするため。解放判定は「全ステージを通し番号で見て、直前のステージがクリア済みなら解放。先頭は常に解放」。

localStorage が使えない環境のフォールバックは 2048 の `local_storage_manager.js`（MIT）の方式に倣う。

- [ ] **Step 1: 失敗するテストを書く**

`tests/ui/storage.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { ProgressStore, mapKV } from '../../src/ui/storage.ts';

describe('ProgressStore', () => {
  let store: ProgressStore;
  beforeEach(() => { store = new ProgressStore(mapKV(new Map())); });

  it('最初は W1-1 だけ解放されている', () => {
    expect(store.isUnlocked('W1-1')).toBe(true);
    expect(store.isUnlocked('W1-2')).toBe(false);
  });

  it('クリアすると次が解放される', () => {
    store.record('W1-1', 3, 1);
    expect(store.isUnlocked('W1-2')).toBe(true);
    expect(store.getStars('W1-1')).toBe(3);
  });

  it('星は下がらない', () => {
    store.record('W1-1', 3, 1);
    store.record('W1-1', 1, 8);
    expect(store.getStars('W1-1')).toBe(3);
    expect(store.bestMoves('W1-1')).toBe(1);
  });

  it('未クリアのステージは星 0', () => {
    expect(store.getStars('W1-3')).toBe(0);
  });

  it('壊れた保存データを読んでも落ちない', () => {
    const kv = mapKV(new Map([['catmaze_progress', '{{{ not json']]));
    expect(() => new ProgressStore(kv)).not.toThrow();
    expect(new ProgressStore(kv).getStars('W1-1')).toBe(0);
  });

  it('配列が入っていても落ちない', () => {
    const kv = mapKV(new Map([['catmaze_progress', '[1,2,3]']]));
    expect(new ProgressStore(kv).getStars('W1-1')).toBe(0);
  });

  it('firstUnclearedId は続きから遊ぶステージを返す', () => {
    expect(store.firstUnclearedId()).toBe('W1-1');
    store.record('W1-1', 2, 3);
    expect(store.firstUnclearedId()).toBe('W1-2');
  });

  it('totalStars は合計を返す', () => {
    store.record('W1-1', 3, 1);
    store.record('W1-2', 2, 3);
    expect(store.totalStars()).toBe(5);
  });

  it('clear で全部消える', () => {
    store.record('W1-1', 3, 1);
    store.clear();
    expect(store.getStars('W1-1')).toBe(0);
    expect(store.isUnlocked('W1-2')).toBe(false);
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- storage`
Expected: FAIL

- [ ] **Step 3: `vite.config.ts` に jsdom 設定を足す**

```ts
test: {
  globals: true,
  environment: 'node',
  environmentMatchGlobs: [['tests/ui/**', 'jsdom']],
  include: ['tests/**/*.test.ts'],
}
```

- [ ] **Step 4: `src/ui/storage.ts` を実装**

`localStorage` の利用可否プローブと fakeStorage フォールバックは 2048 (MIT) の方式。ファイル先頭に由来コメントを書く。JSON のパースは try/catch で包み、結果がプレーンオブジェクトでなければ空として扱う（2048 と phaserlevelselect の双方が備える防御）。

- [ ] **Step 5: `src/ui/router.ts` / `index.html` / `src/main.ts` / `src/styles/base.css` を実装**

`index.html` は `<div id="app"></div>` と viewport meta のみ。`main.ts` で `Router` を作り `#app` の中身を画面ごとに差し替える。ルートは `location.hash`（`#/`, `#/select`, `#/play/W1-1`）で表し、ブラウザの戻るが効くようにする。

- [ ] **Step 6: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 7: コミット**

```bash
git add -A
git commit -m "feat: 進捗ストレージと画面遷移の基盤を追加"
```

---

### Task 11: タイル描画と盤面ビュー

**Files:**
- Create: `src/ui/tile-svg.ts`, `src/ui/board-view.ts`, `src/styles/board.css`
- Test: `tests/ui/tile-svg.test.ts`

**Interfaces:**
- Consumes: `core/types.ts`, `core/conn.ts`, `core/board.ts`
- Produces:
  - `tile-svg.ts`: `tileSvg(tile: Tile): string`（`viewBox="0 0 100 100"` の SVG 文字列）, `catSvg(): string`
  - `board-view.ts`: `class BoardView` — `constructor(root: HTMLElement, board: Board)`,
    `render(state: GameState, opts: { reachable: Set<number>; slidable: Pos[]; hint?: Pos }): void`,
    `onCellClick(cb: (p: Pos) => void): void`, `destroy(): void`

**DOM 構造**（2048 の `.game-container` / `.grid-container` / `.tile-container` を踏襲）:

```html
<div class="board" style="--cols:5; --rows:5">
  <div class="grid-layer"></div>   <!-- 背景のセル枠。z-index 1 -->
  <div class="tile-layer"></div>   <!-- 絶対配置のタイル。z-index 2 -->
  <div class="actor-layer"></div>  <!-- 猫。z-index 3 -->
</div>
```

**タイルの位置**は 2048 の `tile-position-x-y` クラスではなく CSS 変数で表す（盤面サイズが可変のため）:

```css
.tile { transform: translate(calc(var(--c) * 100%), calc(var(--r) * 100%)); }
```

**アニメーション**は 2048 の `html_actuator.js` と同じ二段構え: タイル要素は `Tile.id` をキーに `Map` で使い回し、まず現在の位置で DOM に置いてから `requestAnimationFrame` で新しい `--r` / `--c` を書き込んで CSS transition を起こす。

**パイプ描画**は `net.c` と同じ考え方で、タイル中心から開いている辺の中点へ線を引く。開いている方角が1つだけなら先端に丸を置く。

- [ ] **Step 1: 失敗するテストを書く**

`tests/ui/tile-svg.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { tileSvg } from '../../src/ui/tile-svg.ts';
import { parseConn } from '../../src/core/conn.ts';
import type { Tile } from '../../src/core/types.ts';

const tile = (spec: string, kind: Tile['kind'] = 'road', fixed = false, fish = false): Tile =>
  ({ id: 0, conn: parseConn(spec), kind, fixed, fish });

describe('tileSvg', () => {
  it('開いている方角の数だけ線分を描く', () => {
    expect((tileSvg(tile('NS')).match(/<line/g) ?? []).length).toBe(2);
    expect((tileSvg(tile('NESW')).match(/<line/g) ?? []).length).toBe(4);
    expect((tileSvg(tile('X')).match(/<line/g) ?? []).length).toBe(0);
  });

  it('行き止まりには先端の丸が付く', () => {
    expect(tileSvg(tile('N'))).toContain('tile-cap');
    expect(tileSvg(tile('NS'))).not.toContain('tile-cap');
  });

  it('固定タイルには専用のクラスが付く', () => {
    expect(tileSvg(tile('NS', 'road', true))).toContain('tile-fixed');
  });

  it('ゴールには家のマークを描く', () => {
    expect(tileSvg(tile('NS', 'goal'))).toContain('tile-goal');
  });

  it('固定されたゴールも表現できる', () => {
    const svg = tileSvg(tile('NS', 'goal', true));
    expect(svg).toContain('tile-goal');
    expect(svg).toContain('tile-fixed');
  });

  it('魚があれば魚のマークを描く', () => {
    expect(tileSvg(tile('NS', 'road', false, true))).toContain('tile-fish');
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- tile-svg`
Expected: FAIL

- [ ] **Step 3: `src/ui/tile-svg.ts` を実装**

100×100 の座標系。中心 (50,50) から各方角の辺の中点へ `<line>`。線幅 22、`stroke-linecap="round"`。開いている方角が1つだけなら先端に `<circle class="tile-cap">`。`fixed` は背景に `tile-fixed` の矩形、`goal` は `tile-goal` の家パス、`fish` は `tile-fish` の魚パスを重ねる。色はすべて CSS 変数（`var(--road)` 等）にして CSS 側でテーマを切り替えられるようにする。

- [ ] **Step 4: `src/ui/board-view.ts` と `src/styles/board.css` を実装**

- タイル要素は `Map<number, HTMLElement>`（キーは `Tile.id`）で保持し、`render` のたびに `--r` / `--c` だけ更新する
- 到達領域のセルは背景層に `.reachable` を付けて淡く光らせる（仕様書 §6.2）
- 押せるタイルは `.slidable` を付けてカーソルと縁取りを変える
- 穴は背景層に `.hole` として描く
- `.tile { transition: transform 160ms ease-out }` / `.cat { transition: transform 120ms ease-out }`

- [ ] **Step 5: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 6: コミット**

```bash
git add -A
git commit -m "feat: タイルの SVG 描画と盤面ビューを追加"
```

---

### Task 12: 入力管理と対局画面

**Files:**
- Create: `src/ui/input.ts`, `src/ui/screens/game.ts`, `src/styles/game.css`
- Test: `tests/ui/input.test.ts`

**Interfaces:**
- Consumes: `core/game.ts`, `core/rules.ts`, `solver/search.ts`, `board-view.ts`, `storage.ts`, `router.ts`
- Produces:
  - `input.ts`: `class InputManager` — `constructor(target: EventTarget)`,
    `on(event: 'walk' | 'undo' | 'restart' | 'back', cb: (d?: Dir) => void): void`,
    `bindSwipe(el: HTMLElement): void`, `destroy(): void`
  - `screens/game.ts`: `renderGameScreen(root: HTMLElement, levelId: string, deps: { store: ProgressStore; go: (r: Route) => void }): () => void`（戻り値は後始末関数）

**`input.ts` は 2048 の `keyboard_input_manager.js`（MIT）を TypeScript へ移植したもの**。流用する部分:
- `on` / `emit` のイベントエミッタ
- キーから方向への写像（矢印キー・WASD・vim hjkl）と、修飾キーが押されていたら無視する処理
- スワイプ判定（`touchstart` で開始点を記録し、`touchend` の dx/dy を取り、`Math.max(|dx|,|dy|) > 10` なら大きい方の軸の符号で方向を決める）

2048 の方向は `0=Up, 1=Right, 2=Down, 3=Left` で本作の `Dir`（`0=N, 1=E, 2=S, 3=W`）と一致するのでそのまま使える。`R` のリスタートに加え、`u` / `Backspace` を Undo、`Escape` を戻るに割り当てる。

**入力の曖昧さ解消**は仕様書 §4.3 の優先順位に従う。タップは `BoardView.onCellClick` で受け、`canSlide` なら `slide`、到達領域内なら `walkTo`、それ以外は無視する。

- [ ] **Step 1: 失敗するテストを書く**

`tests/ui/input.test.ts`:

```ts
import { describe, it, expect, vi } from 'vitest';
import { InputManager } from '../../src/ui/input.ts';

describe('InputManager', () => {
  it('矢印キーを方向に変換する', () => {
    const im = new InputManager(document);
    const cb = vi.fn();
    im.on('walk', cb);
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowUp' }));
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowRight' }));
    expect(cb).toHaveBeenNthCalledWith(1, 0);
    expect(cb).toHaveBeenNthCalledWith(2, 1);
    im.destroy();
  });

  it('WASD と hjkl も使える', () => {
    const im = new InputManager(document);
    const cb = vi.fn();
    im.on('walk', cb);
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'a' }));
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'j' }));
    expect(cb).toHaveBeenNthCalledWith(1, 3);
    expect(cb).toHaveBeenNthCalledWith(2, 2);
    im.destroy();
  });

  it('修飾キーが押されていたら無視する', () => {
    const im = new InputManager(document);
    const cb = vi.fn();
    im.on('walk', cb);
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowUp', ctrlKey: true }));
    expect(cb).not.toHaveBeenCalled();
    im.destroy();
  });

  it('u は undo、r は restart を発火する', () => {
    const im = new InputManager(document);
    const undo = vi.fn();
    const restart = vi.fn();
    im.on('undo', undo);
    im.on('restart', restart);
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'u' }));
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'r' }));
    expect(undo).toHaveBeenCalled();
    expect(restart).toHaveBeenCalled();
    im.destroy();
  });

  it('destroy 後はイベントを受け取らない', () => {
    const im = new InputManager(document);
    const cb = vi.fn();
    im.on('walk', cb);
    im.destroy();
    document.dispatchEvent(new KeyboardEvent('keydown', { key: 'ArrowUp' }));
    expect(cb).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- input`
Expected: FAIL

- [ ] **Step 3: `src/ui/input.ts` を実装**

ファイル先頭に由来コメント:

```ts
// キーボードとスワイプの扱いは 2048 の js/keyboard_input_manager.js を元にしている。
// Copyright (c) 2014 Gabriele Cirulli — MIT License
// https://github.com/gabrielecirulli/2048
```

- [ ] **Step 4: `src/ui/screens/game.ts` と `src/styles/game.css` を実装**

画面構成（上から）:
- ヘッダ: 戻るボタン / ステージ名（`W1-2 のりもの`）/ 手数 `てすう 3` と最短 `さいたん 2`
- 盤面（`BoardView`）
- フッタ: `もどす`(Undo) / `やりなおし` / `ヒント` の3ボタン。タップ領域は最低 44px
- クリア時のオーバーレイ（2048 の `.game-message` 構造）: 星3つが1つずつ現れるアニメーション、手数、`つぎへ` / `もういちど` / `ステージせんたく`

`GameSession` を1つ持ち、入力に応じて `walk` / `walkTo` / `slide` / `undo` / `reset` を呼び、そのたびに `BoardView.render` する。`state.cleared` になったら `store.record(levelId, starsFor(def, moves), moves)` を呼んでオーバーレイを出す。

ヒントボタンは `solve(def, def.optimalMoves)` の先頭の手を光らせる（探索深さが `optimalMoves` に限定されているので同期実行で十分速い）。

- [ ] **Step 5: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 6: 実際に動かして確認**

Run: `npm run dev` して `#/play/W1-1` を開き、キーボードとクリックの両方で W1-1〜W1-6 をクリアできることを確認する。

- [ ] **Step 7: コミット**

```bash
git add -A
git commit -m "feat: 入力管理と対局画面を追加"
```

---

### Task 13: ホーム画面とステージ選択画面

**Files:**
- Create: `src/ui/screens/home.ts`, `src/ui/screens/select.ts`, `src/styles/menu.css`
- Test: `tests/ui/select.test.ts`

**Interfaces:**
- Consumes: `levels/index.ts`, `storage.ts`, `router.ts`, `tile-svg.ts`
- Produces: `renderHomeScreen(root, deps): () => void`, `renderSelectScreen(root, deps): () => void`
  （`deps = { store: ProgressStore; go: (route: Route) => void }`）

**ホーム画面**:
- タイトル「ねこめいろ」＋ 猫の SVG
- 主ボタン `つづきから`（`store.firstUnclearedId()` へ）。進捗ゼロなら `はじめる`
- 副ボタン `ステージをえらぶ`
- 総獲得星数 `★ 42 / 96`
- 隅に小さく `きろくをけす`（確認ダイアログ付き）

**ステージ選択画面**:
- ワールドごとのセクション。見出しに `W1 うらにわ` とそのワールドの星数 `★ 14 / 18`
- ステージは 3 列のグリッド。各カードに 番号・名前・星3つ
- 未解放は `.locked` で暗く鍵アイコン。クリックしても遷移しない
- ★3 のカードは縁を強調する

- [ ] **Step 1: 失敗するテストを書く**

`tests/ui/select.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { renderSelectScreen } from '../../src/ui/screens/select.ts';
import { ProgressStore, mapKV } from '../../src/ui/storage.ts';
import { ALL_LEVELS, WORLDS } from '../../src/levels/index.ts';
import type { Route } from '../../src/ui/router.ts';

function setup(store: ProgressStore, go: (r: Route) => void = () => {}) {
  const root = document.createElement('div');
  renderSelectScreen(root, { store, go });
  return root;
}

describe('ステージ選択画面', () => {
  it('全ワールドと全ステージのカードを描く', () => {
    const root = setup(new ProgressStore(mapKV(new Map())));
    expect(root.querySelectorAll('.world')).toHaveLength(WORLDS.length);
    expect(root.querySelectorAll('.level-card')).toHaveLength(ALL_LEVELS.length);
  });

  it('未解放のステージは locked クラスが付く', () => {
    const root = setup(new ProgressStore(mapKV(new Map())));
    const cards = root.querySelectorAll('.level-card');
    expect(cards[0]!.classList.contains('locked')).toBe(false);
    expect(cards[1]!.classList.contains('locked')).toBe(true);
  });

  it('クリア済みのステージは星が点灯する', () => {
    const store = new ProgressStore(mapKV(new Map()));
    store.record('W1-1', 2, 4);
    const root = setup(store);
    expect(root.querySelector('.level-card')!.querySelectorAll('.star.on')).toHaveLength(2);
  });

  it('未解放のカードはクリックしても遷移しない', () => {
    const store = new ProgressStore(mapKV(new Map()));
    let went: Route | null = null;
    const root = setup(store, (r) => { went = r; });
    (root.querySelectorAll('.level-card')[1] as HTMLElement).click();
    expect(went).toBeNull();
    (root.querySelector('.level-card') as HTMLElement).click();
    expect(went).toEqual({ screen: 'play', levelId: 'W1-1' });
  });
});
```

- [ ] **Step 2: テストが落ちることを確認**

Run: `npm test -- select`
Expected: FAIL

- [ ] **Step 3: `src/ui/screens/select.ts` を実装**

- [ ] **Step 4: `src/ui/screens/home.ts` を実装**

- [ ] **Step 5: `src/styles/menu.css` を書く**

配色は CSS 変数で定義し `prefers-color-scheme: dark` で切り替える。カードは `aspect-ratio: 1`、グリッドは `repeat(3, 1fr)`。タップ領域は最低 44px。

- [ ] **Step 6: テストが通ることを確認**

Run: `npm test`
Expected: PASS

- [ ] **Step 7: コミット**

```bash
git add -A
git commit -m "feat: ホーム画面とステージ選択画面を追加"
```

---

### Task 14: 仕上げ

**Files:**
- Modify: `src/styles/*.css`, `src/ui/screens/*.ts`
- Create: `README.md`

- [ ] **Step 1: レスポンシブ対応**

盤面は `min(92vw, 92vh - ヘッダとフッタの高さ, 520px)` に収める。セルの一辺は盤面幅を `--cols` で割って求める。スマホ縦持ちで W5 の 6×6 が指で押せることを確認する。

- [ ] **Step 2: ダークモード対応**

`:root` に明色パレットを定義し、`@media (prefers-color-scheme: dark)` で上書きする。すべての色をトークン経由にして、メディアクエリの中でしか定義されない色を作らない。

- [ ] **Step 3: 演出を足す**

- 猫の歩行 `transform 120ms ease-out`
- スライド `transform 160ms ease-out`
- クリア時に星が1つずつ `scale(0) -> scale(1.15) -> scale(1)` で出る
- 猫がゴールに乗ったら小さく跳ねる
- `prefers-reduced-motion: reduce` ではすべての transition を無効にする

- [ ] **Step 4: `README.md` を書く**

ゲームの説明、遊び方、開発コマンド（`dev` / `test` / `verify` / `play`）、仕様書と実装計画へのリンク、流用元とライセンス（`THIRD_PARTY_NOTICES.md` へのリンク）。

- [ ] **Step 5: 最終確認**

Run: `npm test && npm run verify && npm run build`
Expected: すべて成功

Run: `npm run dev` して W1-1 から W5-8 まで通しで遊べることを確認する。

- [ ] **Step 6: コミット**

```bash
git add -A
git commit -m "feat: レスポンシブ・ダークモード・演出とドキュメントを整備"
```
